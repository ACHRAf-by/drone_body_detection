// File to test detection algorithm

// afterEach function runs after each test suite is executed
afterEach(() => {
    cleanup(); // Resets the DOM after each test suite
})

describe('Detection tests'), () => {
    
    it('Camera activate prompt should launch', () => {
        return
    });
      
  }